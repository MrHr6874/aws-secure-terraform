stages:
  - security

checkov_scan:
  stage: security
  image:
    name: bridgecrew/checkov:latest
    entrypoint: [""]
  script:
    - checkov -d . --check-severity HIGH
  only:
    - main
    - merge_requests

